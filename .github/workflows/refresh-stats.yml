# Refreshes GitHub profile stats (viewcount, contributions, issues, PRs, etc.)
# by fetching from external APIs and committing SVGs to the repo.
# README references these local assets for reliable, up-to-date display.
#
# Schedule: once daily at 06:00 UTC (= 1:00 AM EST; 2:00 AM EDT during DST —
#   GitHub cron is UTC-only and does not observe daylight saving).
# Manual: workflow_dispatch (run on demand any time from the Actions tab).
#
# Upstream SVG hosts (Vercel/Heroku) often return 503 under load; the per-fetch
# curl --retry logic below absorbs transient failures within a single run.

name: Refresh Profile Stats

on:
  schedule:
    - cron: "0 6 * * *"
  push:
    branches: [main]
    paths:
      - ".github/workflows/refresh-stats.yml"
  workflow_dispatch:

concurrency:
  group: refresh-profile-stats
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create assets directory
        run: mkdir -p assets/stats

      - name: Fetch profile stat SVGs
        run: |
          set -euo pipefail
          fetch_svg() {
            local output="$1"
            local url="$2"
            local tmp
            tmp="$(mktemp)"

            if curl -fsSL --connect-timeout 25 --max-time 120 --retry 8 --retry-delay 5 --retry-max-time 300 "$url" -o "$tmp"; then
              if grep -qi "<svg" "$tmp"; then
                mv "$tmp" "$output"
                echo "Updated $(basename "$output")"
                return 0
              fi
              echo "::warning::Non-SVG payload returned for $(basename "$output"); keeping previous file."
            else
              echo "::warning::Fetch failed for $(basename "$output"); keeping previous file."
            fi

            rm -f "$tmp"
            return 1
          }

          successes=0
          failures=0

          # Brand palette (matches michael-kaminski.io): bg #060606, blue #3b82f6 accent,
          # electric-yellow #fff500 highlight, text #e5e7eb, muted #9ca3af.
          if fetch_svg "assets/stats/github-stats.svg" "https://github-readme-stats.vercel.app/api?username=MAKaminski&show_icons=true&include_all_commits=true&count_private=true&rank_icon=github&bg_color=060606&title_color=3b82f6&text_color=e5e7eb&icon_color=fff500&hide_border=true"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/github-streak.svg" "https://github-readme-streak-stats.herokuapp.com/?user=MAKaminski&border_radius=10&background=060606&border=1f2937&stroke=1f2937&ring=3b82f6&fire=fff500&currStreakLabel=3b82f6&sideLabels=e5e7eb&dates=9ca3af&currStreakNum=ffffff&sideNums=ffffff"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/profile-details.svg" "https://github-profile-summary-cards.vercel.app/api/cards/profile-details?username=MAKaminski&theme=github_dark"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/productive-time.svg" "https://github-profile-summary-cards.vercel.app/api/cards/productive-time?username=MAKaminski&theme=github_dark"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/repos-per-language.svg" "https://github-profile-summary-cards.vercel.app/api/cards/repos-per-language?username=MAKaminski&theme=github_dark"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/top-langs.svg" "https://github-readme-stats.vercel.app/api/top-langs/?username=MAKaminski&layout=compact&langs_count=8&bg_color=060606&title_color=3b82f6&text_color=e5e7eb&hide_border=true"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/most-commit-language.svg" "https://github-profile-summary-cards.vercel.app/api/cards/most-commit-language?username=MAKaminski&theme=github_dark"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi
          if fetch_svg "assets/stats/activity-graph.svg" "https://github-readme-activity-graph.vercel.app/graph?username=MAKaminski&bg_color=060606&color=e5e7eb&line=3b82f6&point=fff500&hide_border=true"; then successes=$((successes + 1)); else failures=$((failures + 1)); fi

          echo "Stats refresh summary: success=${successes}, failed=${failures}"
          if [ "$successes" -eq 0 ]; then
            echo "::warning::No stat cards refreshed this run."
          fi

      - name: Fetch Trophies
        continue-on-error: true
        run: |
          set -euo pipefail
          tmp="$(mktemp)"
          if curl -fsSL --connect-timeout 25 --max-time 120 --retry 8 --retry-delay 5 --retry-max-time 300 \
            "https://github-profile-trophy.vercel.app/?username=MAKaminski&theme=algolia&row=2&column=3&title=Followers,Stars,Commits,Repositories,PullRequest,MultiLanguage" \
            -o "$tmp" && grep -qi "<svg" "$tmp"; then
            mv "$tmp" assets/stats/trophies.svg
            echo "Updated trophies.svg"
          else
            rm -f "$tmp"
            echo "::warning::Trophies endpoint unavailable; keeping previous file."
          fi

      - name: Commit and push if changed
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/stats/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: refresh profile stats [automated]"
            if git pull --rebase origin main && git push; then
              echo "Push succeeded"
              exit 0
            fi

            for delay in 4 8 16 32; do
              echo "::warning::Push failed; retrying in ${delay}s"
              sleep "$delay"
              if git pull --rebase origin main && git push; then
                echo "Push succeeded after retry"
                exit 0
              fi
            done

            echo "::warning::All push attempts failed; refresh commit remains local to this run."
          fi
